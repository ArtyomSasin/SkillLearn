export class ColorGroup {
    title: string;
    colors: string[];
    constructor(
        title: string,
        colors: string[]
    ) {
        this.title = title;
        this.colors = colors;
    }
}
